import React from 'react'

const footer = () => {
  return (
    <footer className={styles.wrapper}>

    </footer>
  )
}

export default footer

const styles={
    wrapper:'h-[400px] w-full bg-blue'
}